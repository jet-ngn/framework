{
  "name": "jet-plugins",
  "type": "module",
  "version": "0.0.1-alpha.1",
  "description": "Plugins for Jet UI Framework",
  "main": "index.js",
  "bin": {
    "jet-plugins": "./cli.js"
  },
  "scripts": {
    "setup": "rm -rf ./node_modules && npm install && npm link && echo \"\n\nWARNING: Using local copy of ProductionLine. This should be moved into it's own npm package before final release.\n\n\" && npm link productionline && ./iam && npm run setup",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Graham Butler",
  "license": "ISC"
}
