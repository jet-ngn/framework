const events = {}

export default class DOMEventRegistry {
  static register (node, evt, handler) {
    
  }

  // #handlers = new Map

  // register (node, evt, handler) {
  //   let handlers = this.#handlers.get(node)

  //   if (!handlers) {
  //     this.#handlers.set(node, {
  //       [evt]: [handler]
  //     })

  //     handlers = this.#handlers.get(node)
  //   }

  //   this.#apply()

  //   // if (!this.#handlers.has(node)) {
  //   //   this.
  //   // }

  //   // this.#handlers.set(node, {
  //   //   [evt]: [handler]
  //   // })
  // }
}